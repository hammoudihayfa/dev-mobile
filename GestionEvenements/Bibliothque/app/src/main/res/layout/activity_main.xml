<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#FFFFFF">

    <!-- Logo Image centered at the top -->
    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:srcCompat="@drawable/logo"
        android:contentDescription="Logo" />

    <!-- Space between the logo and buttons -->
    <Space
        android:layout_width="wrap_content"
        android:layout_height="32dp" />

    <!-- Manage Events Button -->
    <Button
        android:id="@+id/manageEventsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:backgroundTint="#7f4aa4"
        android:text="Gestion des Événements"
        android:textColor="#fbee0f" />

    <!-- View Events Button -->
    <Button
        android:id="@+id/viewEventsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:backgroundTint="#7f4aa4"
        android:text="Liste des Événements"
        android:textColor="#fbee0f" />

</LinearLayout>
